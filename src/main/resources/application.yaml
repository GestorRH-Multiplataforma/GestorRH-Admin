spring:
  application:
    name: GestorRH-Admin
  profiles:
    active: dev

server:
  port: 9090
  servlet:
    context-path: /admin

management:
  endpoints:
    web:
      exposure:
        include: "*"
